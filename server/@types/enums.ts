export enum TokenType {
  password_reset = 'password_reset',
  account_verification = 'account_verification',
  login = 'login',
}

export enum Role {
  User = 'User',
  Driver = 'Driver',
  Admin = 'Admin',
}
